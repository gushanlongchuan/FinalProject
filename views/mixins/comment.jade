
mixin comment(user_pic, user_id, user_name, text, current_user_id)
  div.container
    img.img-circle(style="width: 40px;" src='../'+user_pic)
    if (user_id == current_user_id)
       a.b(href='../user/') #{user_name}
    else
       a.b(href='../stranger/'+user_id) #{user_name}
    |  #{text}
